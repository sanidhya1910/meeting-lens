# Core (CPU-only, runs on any machine — no GPU required)
fastapi
uvicorn
python-multipart
faster-whisper
openai
anthropic
pydantic
json-repair

# GPU acceleration is optional. For an NVIDIA GPU, additionally install:
#   pip install -r requirements-gpu.txt
# (and have the CUDA toolkit + cuDNN available). Without it, transcription
# automatically falls back to CPU.
